# Archived Fix Scripts

**Status:** Archived - One-time fixes completed

This directory contains one-time fix scripts that were used during development to correct specific issues. These scripts should **NOT** be run again as they were designed for specific historical problems.

## Scripts in this Directory

- `fix-all-quotes.mjs` - Fixed quote mismatches in card data
- `fix-all-quotes-final.mjs` - Final quote fixing pass
- `fix-all-quote-mismatches.mjs` - Quote mismatch corrections
- `fix-battlebonus-final.mjs` - Battle bonus syntax fixes
- `fix-final-quotes.mjs` - Final quote corrections
- `fix-quotes.mjs` - Initial quote fixes
- `fix-suitbonus-syntax.mjs` - Suit bonus syntax corrections
- `fix-svg-preview.js` - SVG preview structure fixes
- `fix-svg-structure.js` - SVG structure corrections

## Why Archived?

These scripts were created to fix specific issues that occurred during development:

- Quote mismatches in card data files
- Syntax errors in game mechanics
- SVG structure issues

All issues have been resolved, and these scripts are kept for historical reference only.

## ⚠️ Do Not Run

**DO NOT** execute these scripts on the current codebase. They may:

- Overwrite current correct data
- Cause data corruption
- Break existing functionality

If you encounter similar issues, create new fix scripts based on the current codebase structure.

---

**Last Updated:** 2025-01-15
